<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Upload Portal</title>

  <!-- Bootstrap -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">

  <style>
    body {
      background: #f5f7fa;
    }
    .upload-card {
      max-width: 550px;
      margin: 60px auto;
      padding: 30px;
      border-radius: 15px;
      background: white;
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    }
    .title {
      font-weight: 700;
      font-size: 26px;
      margin-bottom: 20px;
      text-align: center;
    }
  </style>
</head>

<body>

  <div class="upload-card">
    <div class="title">Upload Files</div>

    <h5>Upload an Image</h5>
    <form action="/uploadImage" method="POST" enctype="multipart/form-data" class="mb-4">
      <input type="file" name="image" class="form-control mb-3" required>
      <button class="btn btn-primary w-100">Upload Image</button>
    </form>

    <h5>Upload Multiple Files</h5>
    <form action="/uploadFiles" method="POST" enctype="multipart/form-data">
      <input type="file" name="files" class="form-control mb-3" multiple required>
      <button class="btn btn-success w-100">Upload Files</button>
    </form>

  </div>

</body>
</html>
